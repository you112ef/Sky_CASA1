<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Custom Styles for Medical Lab Analyzer -->
    
    <Style x:Key="CustomButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    
    <Style x:Key="CustomTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>
    
    <Style x:Key="CustomLabelStyle" TargetType="Label" BasedOn="{StaticResource MaterialDesignLabel}">
        <Setter Property="Margin" Value="8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>
    
    <!-- RTL Support Styles -->
    <Style x:Key="RTLTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FlowDirection" Value="RightToLeft"/>
        <Setter Property="TextAlignment" Value="Right"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>
    
    <Style x:Key="RTLTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignTextBox}">
        <Setter Property="FlowDirection" Value="RightToLeft"/>
        <Setter Property="TextAlignment" Value="Right"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="8"/>
    </Style>
    
    <Style x:Key="RTLComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignComboBox}">
        <Setter Property="FlowDirection" Value="RightToLeft"/>
        <Setter Property="HorizontalContentAlignment" Value="Right"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="8"/>
    </Style>
    
    <!-- Status Indicator Styles -->
    <Style x:Key="StatusIndicatorStyle" TargetType="Border">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Margin" Value="4"/>
    </Style>
    
    <Style x:Key="NormalStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="#4CAF50"/>
    </Style>
    
    <Style x:Key="AbnormalStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="#FF9800"/>
    </Style>
    
    <Style x:Key="CriticalStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="#F44336"/>
    </Style>
    
</ResourceDictionary>