<Window x:Class="MedicalLabAnalyzer.Views.CalibrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="معايرة الكاميرا" Height="300" Width="520" FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="معايرة الكاميرا/العدسة (Microns per Pixel)" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,8"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,4">
            <TextBlock Text="Microns / Pixel:" Width="160" VerticalAlignment="Center"/>
            <TextBox x:Name="txtMicronsPerPixel" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,4">
            <TextBlock Text="FPS (Frames per second):" Width="160" VerticalAlignment="Center"/>
            <TextBox x:Name="txtFPS" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,4">
            <TextBlock Text="Camera name (اختياري):" Width="160" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCameraName" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="حفظ" Width="100" Margin="6" Click="BtnSave_Click"/>
            <Button Content="إلغاء" Width="100" Margin="6" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>