<UserControl x:Class="MedicalLabAnalyzer.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
    
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#DDD" Direction="270" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2196F3"/>
        </Style>
        
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="لوحة التحكم الرئيسية" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#333"/>
                <TextBlock Text="نظرة عامة على إحصائيات المختبر" 
                           FontSize="16" 
                           Foreground="#666" 
                           Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Statistics Cards -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Total Patients -->
                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalPatients}" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="إجمالي المرضى" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
                
                <!-- Total Exams -->
                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalExams}" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="إجمالي الفحوصات" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
                
                <!-- Today's Exams -->
                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding TodayExams}" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="فحوصات اليوم" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
                
                <!-- Pending Results -->
                <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{Binding PendingResults}" 
                                   Style="{StaticResource StatNumberStyle}"/>
                        <TextBlock Text="نتائج معلقة" 
                                   Style="{StaticResource StatLabelStyle}"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Analysis Statistics -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Analysis Types -->
                <Border Grid.Column="0" 
                        Style="{StaticResource StatCardStyle}" 
                        Margin="10,10,5,10">
                    <StackPanel>
                        <TextBlock Text="أنواع التحاليل" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#333" 
                                   Margin="0,0,0,15"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="تحليل CASA" FontSize="14"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CASAExams}" FontSize="14" FontWeight="Bold"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="تحليل CBC" FontSize="14" Margin="0,10,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CBCExams}" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="تحليل البول" FontSize="14" Margin="0,10,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding UrineExams}" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="تحليل البراز" FontSize="14" Margin="0,10,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding StoolExams}" FontSize="14" FontWeight="Bold" Margin="0,10,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Recent Activity -->
                <Border Grid.Column="1" 
                        Style="{StaticResource StatCardStyle}" 
                        Margin="5,10,10,10">
                    <StackPanel>
                        <TextBlock Text="النشاط الأخير" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#333" 
                                   Margin="0,0,0,15"/>
                        
                        <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding RecentActivity}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,5" Padding="10" Background="#F8F9FA" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Description}" 
                                                           FontSize="12" 
                                                           TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" 
                                                           FontSize="10" 
                                                           Foreground="#666" 
                                                           Margin="0,5,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>